# Workspace Notes

- The repository uses npm workspaces with `apps/api`, `apps/mobile`, and `packages/shared`.
- Keep competition lifecycle and capacity rules in the API; the mobile app consumes the shared contract.
- Run `npm run build` and `npm test` before submitting changes.
